package {{ package }};

{% for imp in imports %}
import {{ imp }};
{% endfor %}

/**
 * Dubbo service interface: {{ interface_name }}
 * Auto-generated by crewai-microservice-builder
 *
 * <p>This interface is exposed as a Dubbo RPC service.
 * Consumers must reference it via {@code @DubboReference}.</p>
 */
public interface {{ interface_name }} {

{% for method in methods %}
    /**
     * {{ method.name }}
     *
{% for param in method.params %}
     * @param {{ param.name }} {{ param.type }}
{% endfor %}
     * @return {{ method.return_type }}
     * @throws IllegalArgumentException if required params are invalid
     */
    {{ method.return_type }} {{ method.name }}({% for param in method.params %}{{ param.type }} {{ param.name }}{% if not loop.last %}, {% endif %}{% endfor %});

{% endfor %}
}
